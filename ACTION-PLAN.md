# SEO 行动计划

## 执行目标

本计划基于 `2026-06-29` 对 `http://test.spiraxsteam.cn/` 的实测审计，优先处理会直接影响索引归属、站点可信度、语言定位和旧站兼容的问题。

## Critical

### 1. 修复 canonical、OG、Schema 的站点归属

- 问题：当前测试域页面统一把规范 URL 指向 `https://www.spiraxsteam.com/`
- 影响：当前域名无法建立独立索引信号，内容权重全部被要求归并到英文正式站
- 目标：按真实业务场景输出正确规范 URL
- 验收标准：
  - `canonical` 与当前公开目标域一致
  - `og:url` 与页面公开 URL 一致
  - `WebPage.url`、`WebSite.url`、`Organization.url` 使用正确站点身份

### 2. 修复 `robots.txt` 与 `sitemap.xml`

- 问题：当前测试域 `robots.txt` 和 `sitemap.xml` 都指向英文正式站
- 影响：抓取与索引信号错误归属
- 目标：让当前站点输出与自身一致的抓取配置
- 验收标准：
  - `robots.txt` 中 `Sitemap:` 指向当前域或目标正式域的正确 sitemap
  - `sitemap.xml` 输出当前站点应提交的 URL 集合
  - sitemap 中 URL 与 canonical 保持一致

### 3. 修复 HTTPS 证书

- 问题：`https://test.spiraxsteam.cn/` 证书域名不匹配
- 影响：严重影响可信度、抓取稳定性和对外访问体验
- 目标：部署匹配 `test.spiraxsteam.cn` 的有效证书
- 验收标准：
  - 浏览器无证书警告
  - `curl -I https://test.spiraxsteam.cn/` 正常通过校验

### 4. 明确站点语言定位并统一输出

- 问题：`.cn` 域名、`zh-CN hreflang`、`html lang="en"`、英文正文、英文页脚同时存在
- 影响：搜索引擎无法准确判断站点语言与地域意图
- 目标：明确这是英文测试站还是中文站，并统一信号
- 验收标准：
  - `html lang`
  - `inLanguage`
  - 页面正文
  - 当前语言显示
  - `hreflang`
  - 五项保持一致

### 5. 恢复旧兼容入口

- 问题：`/search`、`/ajaxcode/msg` 当前为 `404`
- 影响：旧收录、旧外链、旧用户路径失效
- 目标：恢复兼容落点
- 验收标准：
  - `/search` 提供可用页面或 301 到新搜索页
  - `/ajaxcode/msg` 提供兼容消息页或替代落点

## High

### 6. 修复中英混排和模板拼接问题

- 问题：服务页后半段出现中文区块标题，但内容与主体仍是英文
- 影响：页面质量感下降，语言意图混乱
- 目标：同一公开页面避免语言混拼
- 验收标准：
  - 抽样页面无中英模板片段混入
  - 相关内容区语言与页面主语言一致

### 7. 调整 `llms.txt` 输出策略

- 问题：`llms.txt` 为英文正式站提供引用路径，且主链接大量使用 `.md`
- 影响：AI 引用与规范化信号不统一
- 目标：让 `llms.txt` 与真实站点归属一致
- 验收标准：
  - HTML Source 指向正确站点
  - 主链接优先使用公开 HTML URL
  - 与 canonical 保持一致

### 8. 优化空新闻页策略

- 问题：`/news/` 当前无内容
- 影响：栏目价值弱，容易被视为占位页
- 目标：空栏目不成为主要 SEO 资产页
- 验收标准：
  - 保持 `noindex, follow` 并确认不进正式 sitemap
  - 或补齐真实新闻内容后再恢复索引

## Medium

### 9. 精简联系页重复文案

- 问题：联系页存在重复 highlight 文案
- 影响：页面质量和转化表达略显冗余
- 目标：提升可读性与信息密度
- 验收标准：
  - 重复模块文案去重
  - 每个卡片表达不同价值点

### 10. 收敛图片与预加载策略

- 问题：首页和联系页预加载图片较多，且大量资源仍为 `http`
- 影响：性能和资源一致性存在风险
- 目标：减少非必要预加载并统一到 `https`
- 验收标准：
  - 首屏仅保留必要预加载图
  - 公开页面图片资源走 `https`
