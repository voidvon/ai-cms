# ACTION PLAN

## 执行目标

本行动计划基于 2026-06-26 对 `http://localhost:1231/` 的全站 SEO 审计结果，按 `Critical > High > Medium > Low` 排序，优先解决会直接影响索引、规范化和旧站兼容的问题。

---

## Critical

### 1. 修复产品详情页 canonical 错误

- 问题：slug 详情页的 canonical 指向数字 ID URL，而该 URL 返回 `404`
- 影响：严重影响索引归并，可能导致正确页面不被稳定收录
- 目标：所有公开详情页必须自 canonical 到自身公开 slug URL
- 验收标准：
  - 详情页 HTML 中 `canonical` 与当前公开 URL 完全一致
  - 历史数字 ID URL 若存在，应返回 `301` 到 slug URL
  - sitemap 中仅保留 slug URL
- 预计优先级：立即处理
- 建议负责人：模板运行时 / URL 生成 / 页面渲染链路

### 2. 清理 sitemap 中的重复 URL

- 问题：`/` 与 `/index.html`、栏目路径与 `/index.html` 版本同时进入 sitemap
- 影响：抓取预算浪费，重复页信号增强，canonical 冲突加重
- 目标：sitemap 只输出规范 URL
- 验收标准：
  - `sitemap-1.xml` 中不再出现 `/index.html` 类 URL
  - 同一内容仅保留一个最终 URL
  - sitemap 与 canonical 一致
- 预计优先级：立即处理
- 建议负责人：静态生成 / sitemap 服务 / 路径服务

### 3. 修复 OG / Schema 图片资源地址

- 问题：页面元数据与结构化数据中的图片资源使用 `http://localhost:1232/`
- 影响：搜索引擎、社交平台和 AI 抓取器无法将其视为正式公开资源
- 目标：所有面向爬虫的图片 URL 统一使用正式公开域名或稳定公开路径
- 验收标准：
  - `og:image`
  - `twitter:image`
  - `primaryImageOfPage`
  - `Product.image`
  - 均不再使用 `localhost:1232`
- 预计优先级：立即处理
- 建议负责人：站点配置 / 资源 URL 生成 / 模板运行时

### 4. 处理空新闻页索引问题

- 问题：`/news/` 已索引、已进入 sitemap，但当前文章数为 0
- 影响：形成薄内容页，拖累整体质量信号
- 目标：在有真实内容前，不让空栏目页成为主索引目标
- 可选方案：
  - 方案 A：对空新闻页加 `noindex`
  - 方案 B：从 sitemap 中移除该页
  - 方案 C：补充至少首批真实新闻内容后再恢复索引
- 验收标准：
  - 空栏目页不再被主动提交抓取
  - 或页面内容达到可索引标准
- 预计优先级：立即处理
- 建议负责人：内容管理 / 栏目渲染 / sitemap 服务

---

## High

### 5. 恢复旧站兼容入口

- 问题：`/search`、`/ajaxcode/msg` 当前返回 `404`
- 影响：历史外链、旧收录、旧用户路径失效
- 目标：恢复旧入口兼容能力，至少做到可用落地或明确跳转
- 验收标准：
  - `/search` 返回可用搜索页或跳转到新搜索页
  - `/ajaxcode/msg` 返回兼容落点或替代页
  - 旧路径不再直接 404
- 预计优先级：1 周内
- 建议负责人：兼容路由 / 路由层 / 静态兼容入口

### 6. 移除后台入口的开发端口跳转

- 问题：`/admin/` 当前 `302` 到 `http://127.0.0.1:5173/admin/`
- 影响：部署环境暴露开发配置，属于工程质量风险
- 目标：后台入口只指向实际可访问后台地址
- 验收标准：
  - `/admin/` 不再跳转到 `127.0.0.1:5173`
  - 预览 / 生产环境后台地址与部署配置一致
- 预计优先级：1 周内
- 建议负责人：服务端路由 / 管理后台配置

### 7. 修正 `llms.txt` 的 URL 策略

- 问题：`llms.txt` 中大量链接指向 `.md` 资源，而非公开 canonical HTML URL
- 影响：AI 引用路径不统一，弱化规范化信号
- 目标：`llms.txt` 统一使用正式 HTML 公开地址
- 验收标准：
  - 去除 `.md` 版本外链
  - 核心页面、栏目页、详情页使用公开 HTML URL
  - 与页面 canonical 保持一致
- 预计优先级：1 周内
- 建议负责人：AI 搜索输出 / 站点导出逻辑 / 内容索引脚本

---

## Medium

### 8. 强化栏目页和知识页元描述

- 问题：部分页面 `meta description` 过于泛化，例如 `Learn about steam`
- 影响：CTR 与页面主题表达能力不足
- 目标：为栏目页、知识页、列表页生成更具主题性的说明
- 验收标准：
  - 每个主要栏目页具有独特、可读、可点击的描述
  - 描述中体现主题范围和用户价值
- 预计优先级：1 个月内
- 建议负责人：SEO 元数据模板 / 栏目模板绑定

### 9. 优化联系页内容精炼度

- 问题：联系页部分 highlight 模块文案重复
- 影响：削弱页面专业感和内容密度
- 目标：减少重复表达，让每个模块承载独立信息点
- 验收标准：
  - 重复文案被替换为差异化信息
  - 联系页内容结构更紧凑
- 预计优先级：1 个月内
- 建议负责人：单页模板 / 内容编辑

### 10. 进行真实性能复测

- 问题：当前只有结构判断，没有真实 CWV / Lighthouse 数据
- 影响：性能优化无法精确排序
- 目标：补齐性能基线，为资源优化提供数据依据
- 验收标准：
  - 输出首页、联系页、产品详情页、知识栏目页的 Lighthouse 数据
  - 补充 LCP / INP / CLS 基线
- 预计优先级：1 个月内
- 建议负责人：前端性能 / SEO 技术检查

---

## Low

### 11. 扩充新闻板块内容量

- 目标：让新闻栏目恢复为真实可索引内容资产
- 建议：
  - 补充 5-10 篇基础新闻或公司动态
  - 每篇文章配置唯一标题、摘要、发布时间、封面图和相关推荐

### 12. 继续增强产品详情页结构化表达

- 目标：提升产品页在搜索与 AI 引用中的可理解性
- 建议：
  - 增加 FAQ 模块
  - 完善应用场景、适用边界、替换型号、下载资料关系
  - 视业务情况增强 Product schema 字段

---

## 推荐执行顺序

### 第一阶段：立即修复（本周）

1. canonical 修复
2. sitemap 去重
3. 图片资源地址修复
4. 空新闻页索引处理

### 第二阶段：兼容与治理（1 周内）

1. `/search` 兼容恢复
2. `/ajaxcode/msg` 兼容恢复
3. `/admin/` 开发跳转修复
4. `llms.txt` URL 规范化

### 第三阶段：优化增强（1 个月内）

1. 栏目页元描述增强
2. 联系页文案优化
3. 性能复测与资源优化
4. 新闻内容补充

---

## 修复完成后的复检清单

- 检查首页 `/` 的 canonical、OG、schema 图片地址
- 检查产品详情页 canonical 是否自洽
- 检查数字 ID 旧 URL 是否 `301` 到 slug URL
- 检查 `sitemap.xml` 与 `sitemap-1.xml` 是否只保留规范 URL
- 检查 `/news/` 是否仍为索引态空页
- 检查 `/search` 与 `/ajaxcode/msg` 是否恢复兼容
- 检查 `/admin/` 是否仍暴露本地开发地址
- 检查 `llms.txt` 是否全部使用公开 HTML URL

## 预期结果

若按本计划完成 Critical 与 High 项，站点 SEO 状态预计可从当前 `66/100` 提升到 `78-82/100` 区间，核心改善将体现在：

- 收录稳定性提升
- 重复 URL 问题下降
- canonical 信号恢复正常
- 旧站兼容性增强
- 社交与搜索抓取资源一致性提高
- AI 搜索引用路径更清晰
